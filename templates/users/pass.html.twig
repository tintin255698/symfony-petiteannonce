{% extends 'users.html.twig' %}

{% block title %}Hello UsersController!{% endblock %}

{% block body %}

    <div class="grix xs12 container">
        <div class="col-xs12"><h1> Modifier password de {{ app.user.name }} {{ app.user.firstname }}</h1></div>
        <form method="post">
            <div class="form-field">
                <input type="password" id="pass" name="pass" class="form-control rounded-1" />
                <label for="pass">Mot de passe</label>
            </div>
            <div class="form-field">
                <input type="password" id="pass2" name="pass2" class="form-control rounded-1" />
                <label for="pass2">Modifier mot de passe</label>
            </div>
        </form>
    </div>
{% endblock %}
